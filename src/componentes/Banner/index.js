import './Banner.css'

const Banner = () => {
    //JSX - Como o react insere elementos no DOM
    return (
        <header className="banner">
            <img src="/imagens/banner.png" alt="Banner principal da página do Organo"/>
        </header>
    )
}

export default Banner